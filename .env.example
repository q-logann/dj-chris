# Server-only — never expose to client
RESEND_API_KEY=re_your_resend_api_key_here
MOM_BOOKING_EMAIL=bookings@example.com

# Public — safe to expose to client
NEXT_PUBLIC_SITE_URL=http://localhost:3000
